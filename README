# lotus-stencila

1. `npm install @stencila/dockta` not working on my side
2. run `docker build . -t adafede/lotus-stencila` instead
3. push the container image to a Docker container registry (here `docker push adafede/lotus-stencila`)
4. set the container image as execution environment for the project (`adafede/lotus-stencila:latest`)
5. ... not working
